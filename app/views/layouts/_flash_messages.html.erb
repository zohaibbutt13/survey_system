<% flash.each do |key, value| %>
    <% if Array === value %>
    <% value.each do |val| %>
      <div class="<%= flash_type(key) %>">
        <%= val %>
      </div>
    <% end %>
    <% else %>
      <div class="<%= flash_type(key) %>">
        <%= value %>
      </div>
    <% end %>
<% end %>