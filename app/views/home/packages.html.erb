<!-- Packages -->
<section class="packages text-center bg-light">
  <div class="container">
    <h2 class="mb-5">Choose from one of our best packages</h2>
    <div class="row">
      <!-- UPDATE: Replace dummy name parameter with the attributes of package object -->
      <% @packages.each do |name| %>
      <%= render 'layouts/package', package_name: name %>
      <% end %>
    </div>
  </div>
</section>
