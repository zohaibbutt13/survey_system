$('#responses').html("<%= escape_javascript render(partial: 'responses') %>");
 var responses_button = document.getElementById('see_responses');
 responses_button.parentNode.removeChild(responses_button);