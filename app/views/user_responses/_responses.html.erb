<% if @survey.survey_type == 'Public' %>
	<% @response_per_page = @response_per_page.unscoped.where(survey_id: @survey.id) %>
<% end %>
<%= render partial: "response", collection: @response_per_page, as: :user_response %>